---
import Base from "@layouts/Base.astro";
import Banner from "@layouts/components/Banner.astro";
import KeyFeatures from "@layouts/components/KeyFeatures.astro";
import ReferensiDokumen from "@components/referensidokumen.astro";
// import Testimonial from "@layouts/components/Testimonial.astro";
import Counter from "@layouts/components/counter.astro";
// import Service from "@layouts/components/Service.astro";
// import Cta from "@layouts/partials/Cta.astro";
import { getEntryBySlug } from "astro:content";

const homepage = await getEntryBySlug("homepage", "index");
const {
  banner,
  counter,
  key_features,
  referensidokumen,
  service,
  testimonial,
} = homepage.data;
---

<Base>
  <!-- banner -->
  <Banner banner={banner} />

  <!--Counter -->
  <Counter counter={counter} />

  <!-- key features -->
  <KeyFeatures key_features={key_features} />

  <!-- referensidokumen -->
  <ReferensiDokumen referensidokumen={referensidokumen} />

  <!-- service -->
  <!-- <Service service={service} /> -->
  <!-- testimonial -->
  <!-- <Testimonial testimonial={testimonial} /> -->
  <!-- call to action -->
  <!-- <Cta /> -->
</Base>
